# Accessibility — §32 / WCAG AA

- Color contrast: dark hero/footer vs light sections verified against tokens (≥4.5:1 body, ≥3:1 large).
- Keyboard: Tab/Enter/Escape/Arrows on nav/menus, visible `:focus-visible` (2px solid engineering, offset 2px).
- Semantics: `role="navigation"`, `aria-expanded/haspopup`, `aria-label`, landmark `header/main/footer`, skip link, single `h1` per page.
- Images: `alt` on all, `aria-hidden` on decorative SVG.
- Motion: `@media (prefers-reduced-motion: reduce)` disables animations site-wide.
- Forms: (coming-soon has no forms yet) — pattern ready with labels/ARIA.

Manual check: `npx ng build` passes; no console errors; prerender HTML is readable without JS for core content.
